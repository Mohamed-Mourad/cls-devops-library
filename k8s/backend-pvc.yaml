apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-pvc
  namespace: k8s
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: eks-efs-sc
  resources:
    requests:
      storage: 1Gi
